# Priority-Dependency-Task-Scheduler
This project uses a graph (adjacency list) to manage task dependencies and a priority queue (max-heap) to schedule tasks by priority. It performs cycle detection to avoid deadlocks, topological sorting to find the optimal execution order, and generates alerts for missed deadlines.
